# Dependencies
node_modules/

# Environment (secrets — keep .env.example tracked)
.env
.env.local
.env.*.local

# Logs & debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS
.DS_Store
Thumbs.db
Desktop.ini

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Optional tooling
coverage/
.nyc_output/

# Docker (optional local overrides)
docker-compose.override.yml

# ── Güvenlik: hassas veri içeren dosyalar ────────────────
# Veritabanı dökümleri kullanıcı e-postaları, bcrypt hash'leri ve
# CTF flag'lerini içerir — depoya girmemeli.
*.sql
!agdsecscop_team/server/db/schema.sql
agdsecscop_team/Database/

# Yüklenen dosyalar (öğrenci/ders materyalleri, CTF ekleri)
agdsecscop_team/server/uploads/
